# QOR Shell Migration — victor-shell → /qor route tree

Migrated all victor-shell features surfaces dedicated route-backed surfaces under `/qor/`:

- Theme system (6 themes + settings drawer) → `/qor`
- CadenceSelector → `/qor/victor`  
- VictorOperatorConsole (status + chat) → `/qor/victor`
- VictorGovernancePage → `/qor/victor/governance`
- VictorAuditPage (run feed + payloads) → `/qor/victor/audit`
- ForgeConstellation (canvas + physics + 3D) -> `/qor/forge/constellation`
- ForgeWorkspace (projects + roadmap + risks) -> `/qor/forge/*`
- Mobile responsive CSS on all showcase + operational routes

Substantiation: 12/12 routes verified (2026-03-31).
